<div class="container mt-5 text-center">
    <div class="jumbotron shadow-sm">
        <h1 class="font-weight-light">Generate Short URLs for Free!</h1>
        <p>A free and open-source url shortner that magically generates unique short links everytime.</p>
        <br /><br />
        <div class="container">
            <div class="m-auto mt-5">
                <div class="input-group mb-3">
                    <div class="input-group-prepend">
                        <button type="button" class="btn btn-success" id="shortBtn">
                            short it! <span data-feather="scissors"></span>
                        </button>
                    </div>
                    <input id="longUrl" type="url" class="form-control py-4" placeholder="Paste your URL..." />
                </div>
            </div>
            <div class="m-auto mt-5">
                <div class="input-group mb-3">
                    <div class="input-group-prepend">
                        <button
                            type="button"
                            class="btn btn-primary"
                            id="copyBtn"
                            data-clipboard-action="copy"
                            data-clipboard-target="#finalUrl"
                        >
                            copy it! <span data-feather="clipboard"></span>
                        </button>
                    </div>
                    <input
                        type="url"
                        class="form-control py-4 bg-white"
                        value=""
                        placeholder="Unique Link..."
                        id="finalUrl"
                        readonly
                    />
                </div>
            </div>
        </div>
    </div>
</div>
